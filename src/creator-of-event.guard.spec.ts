import { CreatorOfEventGuard } from './creator-of-event.guard';

describe('CreatorOfEventGuard', () => {
  it('should be defined', () => {
    expect(new CreatorOfEventGuard()).toBeDefined();
  });
});
